import * as React from 'react';
import { View, Text } from 'react-native';

const CallLogs = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center'}}>
          <Text style={{color: "black"}}>CallLogs Screen</Text>
        </View>
      );
}

export default CallLogs