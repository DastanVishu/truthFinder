import * as React from 'react';
import { View, Text } from 'react-native';

const Recoding = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center'}}>
          <Text style={{color: "black"}}>Recoding Screen</Text>
        </View>
      );
}

export default Recoding